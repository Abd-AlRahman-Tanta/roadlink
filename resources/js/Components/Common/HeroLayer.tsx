
const HeroLayer = ({ className }: { className?: string }) => {
  return (
    <div className={`${className}  heroLayer w-full h-full absolute top-0 left-0 `}>

    </div>
  )
}

export default HeroLayer
