
const RegularLayer = ({ className }: { className?: string }) => {
  return (
    <div className={` regularLayer ${className} w-full h-full absolute top-0 left-0 `}>

    </div>
  )
}

export default RegularLayer
